Adding dependency check for CLUTO/vcluster and libgd. Adding this to documentation. 

Adding the Standard error bars to the graphs.

Adding incremental confidence interval values to the log file.

README: providing statistics about the capacity of our modules - file size and time expected on some system.

README: Detailed explanation of the output files (png, text, log)

using the -d:DProf option to understand where your program is spending it's time.

Separating the functionality into smaller reusable Perl modules

Add the support for Order2 context representation.

